pipeline {
    agent any
    tools { 
        maven 'Maven' 
        jdk 'Java_Home'
    }
   
        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
   }
